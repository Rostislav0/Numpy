import numpy as np

# Y = np.array([[99, 11, 55], [33, 66, 99]])
# Z = np.around((Y-np.average(Y))/np.std(Y), 2)
# print(Z)
# std -- standard deviation

A = np.array([1.5, 2.5, 3.5])
B = np.array([4, 5, 6])
Z = np.dot(A, B)  # dot -- sum(A * B)
print(Z)
